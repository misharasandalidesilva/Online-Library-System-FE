export interface Book {
    _id: string;
    title: string;
    author: string;
    description: string;
    category: string;
    publisher: string;
    publishedDate: Date;
    quantity: number;
    timeStamp: Date;
}
